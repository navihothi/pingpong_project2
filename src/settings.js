export const SVG_NS = 'http://www.w3.org/2000/svg';

export const KEYS = {
    a: 65,          //player 1 up key
    z: 90,          //player 1 down key
    up: 38,         //player 2 up key
    down: 40,       //player 2 down key
    spaceBar: 32,   //to start the game
}


/*document.addEventListener('keydown', event => {
    //console.log(event);
});

document.addEventListener('keyup', event => {
        //console.log(event);
});

document.addEventListener('Space', event => {
        //console.log(event);
});

document.addEventListener('KeyW', event => {
        //console.log(event);
});

document.addEventListener('KeyS', event => {
        //console.log(event);
});*/
